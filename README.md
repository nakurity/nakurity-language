# nakurity-language
is the laziest language when it comes to its structure. Nakurity Lang has nothing loaded by default, not even its own native import library. 
All it has is a namespace resolver, that imports c files and not its own native files.
